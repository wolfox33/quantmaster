# `permutation_entropy`

```python
from quantmaster.features.entropy import permutation_entropy

df["perm_entropy_50"] = permutation_entropy(df, window=50, order=3, delay=1)
```

::: quantmaster.features.entropy.permutation_entropy
