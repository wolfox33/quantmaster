# `shannon_entropy`

```python
from quantmaster.features.entropy import shannon_entropy

df["shannon_entropy_50"] = shannon_entropy(df, window=50, bins=10)
```

::: quantmaster.features.entropy.shannon_entropy
